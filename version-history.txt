
v4 (development)

- to do

v3 (current release)

- fix item loss + other fixes
- adds disable inventory sorting (as suggested by SkitAsul at github.com)
- disable sorting if permission 'clicksortlite' is set to false
- disable sorting if InventoryClickEvent is cancelled
- set InventoryClickEvent priority to highest for these to work (don't do the same)

v2 

- better documentation througth code comments
- some performance improvements (break loops timely)
- added plugin description shown in /help
- fixed exception with items in 2nd half large chests
- fixed item loss: pumpkins, phantom membrane, nautilus shell, etc

v1

- initial release
- craftbukkit 1.13+ support
